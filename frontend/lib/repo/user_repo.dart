import 'package:supabase_flutter/supabase_flutter.dart';

class UserRepo {
  final GoTrueClient supabaseAuth;

  UserRepo(this.supabaseAuth);
  //function which will take the user ID and check if the user is present in the database
  // if present return the USER object 
  // if not present return null
  

  // function to write 
}